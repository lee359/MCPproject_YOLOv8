# YOLOv8 + MCP + Claude 整合系統實驗方法設計
**版本：v1.0**  
**日期：2025-11-06**  
**專題主題：探討 YOLOv8 模型辨識通過 MCP 連接 Claude 進行辨識數據整理**

---

## 📋 目錄
1. [實驗目標](#實驗目標)
2. [實驗設計原則](#實驗設計原則)
3. [實驗架構](#實驗架構)
4. [實驗流程](#實驗流程)
5. [數據記錄方法](#數據記錄方法)
6. [評估指標](#評估指標)
7. [預期成果](#預期成果)

---

## 🎯 實驗目標

### 核心研究問題
本研究旨在評估 **YOLOv8 + MCP + Claude** 整合系統在實時物體辨識與數據整理上的能力，具體探討：

1. **MCP 工具效能**：不同 MCP 工具（`detect_esp32_stream`、`detect_stream_frame_simple`）的處理效率
2. **Claude 數據整理能力**：Claude 如何理解、分析和整理 YOLOv8 的偵測結果
3. **系統整合優勢**：相較於傳統純 YOLOv8 方案，整合 Claude 後的附加價值
4. **實用性評估**：系統在實際應用場景（如 ESP32-CAM 監控）的可行性

### 研究範疇界定
- ✅ **聚焦於**：MCP 架構、Claude 分析能力、系統整合效能
- ❌ **不涉及**：YOLOv8 在不同光照/天氣條件下的辨識率（純 CV 研究範疇）
- 🔧 **實驗環境**：固定 ESP32-CAM 位置，正常室內光照條件

---

## 🔬 實驗設計原則

### 1. 變數控制
- **自變數**：場景複雜度、Claude 任務類型、MCP 工具選擇
- **依變數**：辨識準確度、處理時間、Claude 回應品質
- **控制變數**：光照條件、相機位置、網路環境

### 2. 重複性原則
- 每個實驗條件重複 **10 次**
- 總實驗次數：3 場景 × 4 任務 × 10 次 = **120 組數據**

### 3. 客觀性原則
- 使用結構化數據記錄
- 引入人工驗證（Ground Truth）
- 自動計算準確率指標

---

## 🏗️ 實驗架構

### 實驗組別設計

#### **實驗一：場景複雜度測試**
測試系統在不同複雜度場景下的表現

| 場景類型 | 描述 | 測試次數 |
|---------|------|---------|
| **場景 A：單一物體** | 單隻貓或單隻狗 | 貓 10 次 + 狗 10 次 |
| **場景 B：雙物體** | 貓和狗同時出現 | 10 次 |
| **場景 C：複雜場景** | 多隻動物 + 背景物體 | 10 次 |

**測試目標**：
- YOLOv8 在不同場景的偵測準確度
- Claude 能否正確理解複雜場景的偵測結果

---

#### **實驗二：Claude 數據整理能力測試**
固定使用場景 A 和 B，測試 Claude 在不同任務下的表現

| 任務類型 | Claude 提示詞範例 | 評估重點 |
|---------|------------------|---------|
| **任務 A：基礎描述** | 「請描述偵測到的物體及其位置」 | 描述準確性、完整性 |
| **任務 B：數據統計** | 「請統計偵測結果並製作表格」 | 統計正確性、格式規範 |
| **任務 C：趨勢分析** | 「根據連續 5 次偵測，分析物體移動趨勢」 | 分析能力、邏輯推理 |
| **任務 D：異常檢測** | 「請指出可能的誤判或異常偵測」 | 錯誤識別、專業判斷 |

**測試目標**：
- Claude 理解 YOLOv8 輸出格式的能力
- 不同提示詞對 Claude 回應品質的影響

---

#### **實驗三：MCP 工具效能比較**
比較三種 MCP 工具的處理效能

| 工具名稱 | 特點 | 測試場景 |
|---------|------|---------|
| `detect_esp32_stream` | 完整版 + Kalman Filter 追踪 | 場景 A, B, C 各 10 次 |
| `detect_stream_frame_simple` | 簡化版，不返回圖像 | 場景 A, B, C 各 10 次 |
| `detect_image` | 靜態圖片偵測 | 場景 A 10 次 |

**測試目標**：
- 各工具的處理時間差異
- Kalman Filter 對追踪穩定性的影響
- 簡化版工具的效能提升幅度

---

## 📊 實驗流程

### 階段一：環境準備（預計 15 分鐘）
```
1. 硬體設置
   ├─ 固定 ESP32-CAM 於穩定位置（三腳架/固定架）
   ├─ 確認串流 URL 可訪問 (http://192.168.0.104:81/stream)
   ├─ 測試網路延遲 (ping 測試)
   └─ 確認 Claude Desktop 與 MCP 連線正常

2. 軟體配置
   ├─ 啟動 MCP 服務器：python mcpclient.py
   ├─ 驗證工具可用性：check_stream_health()
   └─ 初始化實驗記錄系統

3. 校準測試
   ├─ 執行 3 次測試確認系統穩定
   └─ 記錄基準效能數據
```

### 階段二：實驗一執行（預計 60 分鐘）
```
場景 A：單一物體（20 次）
├─ 準備階段：擺放單隻貓
├─ 執行偵測：呼叫 detect_esp32_stream
├─ Claude 分析：使用任務 A 提示詞
├─ 數據記錄：自動儲存至 CSV
└─ 重複 10 次後，更換為單隻狗

場景 B：雙物體（10 次）
└─ 同上流程

場景 C：複雜場景（10 次）
└─ 同上流程
```

### 階段三：實驗二執行（預計 40 分鐘）
```
固定使用場景 A（單隻貓）
├─ 任務 A：基礎描述（10 次）
├─ 任務 B：數據統計（10 次）
├─ 任務 C：趨勢分析（連續 5 次為一組，共 2 組）
└─ 任務 D：異常檢測（10 次）
```

### 階段四：實驗三執行（預計 30 分鐘）
```
使用場景 A, B, C
├─ detect_esp32_stream（30 次）
├─ detect_stream_frame_simple（30 次）
└─ detect_image（10 次）
```

### 階段五：數據處理與分析（預計 60 分鐘）
```
1. 數據清理與驗證
2. 計算評估指標
3. 生成視覺化圖表
4. 撰寫實驗報告
```

---

## 💾 數據記錄方法

### 自動化記錄系統

```python
class MCPExperimentLogger:
    """
    MCP 實驗數據記錄器
    自動記錄每次實驗的完整數據
    """
    
    def __init__(self, experiment_name="MCP_YOLOv8_Claude_Experiment"):
        self.experiment_name = experiment_name
        self.data = []
        self.start_time = datetime.now()
    
    def log_trial(self, scene_type, task_type, trial_num, 
                  yolo_result, claude_prompt, claude_response,
                  ground_truth=None):
        """記錄單次實驗數據"""
        
        # 提取 YOLOv8 偵測結果
        detections = yolo_result.get("detections", [])
        
        record = {
            # ===== 基本資訊 =====
            "timestamp": datetime.now().isoformat(),
            "experiment_name": self.experiment_name,
            "scene_type": scene_type,  # "single_cat", "cat_dog", "complex"
            "task_type": task_type,    # "description", "statistics", etc.
            "trial_number": trial_num,
            
            # ===== YOLOv8 偵測結果 =====
            "yolo_detection_count": yolo_result["detection_count"],
            "yolo_detected_classes": [d["class"] for d in detections],
            "yolo_confidences": [d["confidence"] for d in detections],
            "yolo_confidence_avg": np.mean([d["confidence"] for d in detections]) if detections else 0,
            "yolo_confidence_std": np.std([d["confidence"] for d in detections]) if detections else 0,
            "yolo_bboxes": [d["bbox"] for d in detections],
            
            # ===== MCP 工具資訊 =====
            "mcp_tool_used": yolo_result.get("tool_name", "detect_esp32_stream"),
            "stream_url": yolo_result.get("stream_url", ""),
            "frame_width": yolo_result["frame_size"]["width"],
            "frame_height": yolo_result["frame_size"]["height"],
            
            # ===== 效能指標 =====
            "total_time": yolo_result["performance"]["total_time"],
            "yolo_inference_time": yolo_result["performance"].get("yolo_inference_time", 0),
            "frame_read_time": yolo_result["performance"].get("frame_read_time", 0),
            "kalman_predict_time": yolo_result["performance"].get("kalman_predict_time", 0),
            "base64_encode_time": yolo_result["performance"].get("base64_encode_time", 0),
            
            # ===== Claude 互動 =====
            "claude_prompt": claude_prompt,
            "claude_response": claude_response,
            "claude_response_length": len(claude_response),
            "claude_response_word_count": len(claude_response.split()),
            
            # ===== 人工驗證 =====
            "ground_truth": ground_truth,  # 人工標註的真實結果
            "is_correct": None  # 事後計算
        }
        
        self.data.append(record)
        return record
    
    def save_to_csv(self, filename=None):
        """儲存為 CSV 檔案"""
        if filename is None:
            filename = f"{self.experiment_name}_{datetime.now().strftime('%Y%m%d_%H%M%S')}.csv"
        
        df = pd.DataFrame(self.data)
        df.to_csv(filename, index=False, encoding='utf-8-sig')
        print(f"✅ 數據已儲存至：{filename}")
        return filename
    
    def get_summary_statistics(self):
        """生成統計摘要"""
        df = pd.DataFrame(self.data)
        
        summary = {
            "total_trials": len(df),
            "avg_detection_count": df["yolo_detection_count"].mean(),
            "avg_confidence": df["yolo_confidence_avg"].mean(),
            "avg_total_time": df["total_time"].mean(),
            "avg_yolo_time": df["yolo_inference_time"].mean(),
            
            "by_scene": df.groupby("scene_type").agg({
                "yolo_detection_count": ["mean", "std"],
                "yolo_confidence_avg": ["mean", "std"],
                "total_time": ["mean", "std"]
            }),
            
            "by_task": df.groupby("task_type").agg({
                "claude_response_length": ["mean", "std"],
                "claude_response_word_count": ["mean", "std"]
            })
        }
        
        return summary
```

### 記錄檔案格式

**CSV 檔案結構**（部分欄位）：
```
timestamp, scene_type, task_type, trial_number, yolo_detection_count, 
yolo_confidence_avg, total_time, claude_response, ground_truth, is_correct
```

---

## 📈 評估指標

### 1. YOLOv8 偵測指標
- **準確率 (Accuracy)**：正確偵測次數 / 總次數
- **平均信心分數**：所有偵測的平均 confidence
- **漏檢率 (False Negative Rate)**：未偵測到物體的比例
- **誤檢率 (False Positive Rate)**：錯誤偵測的比例

### 2. Claude 分析指標
- **描述完整性**：Claude 是否提及所有偵測到的物體
- **統計準確性**：Claude 統計的數量是否與 YOLOv8 一致
- **回應時間**：（需手動計時或使用 Claude API）
- **回應品質評分**：人工評分（1-5 分）

### 3. 系統效能指標
- **端到端延遲**：從偵測開始到 Claude 回應完成的總時間
- **YOLOv8 推論時間**：模型推論耗時
- **MCP 傳輸開銷**：總時間 - 推論時間
- **吞吐量 (Throughput)**：每秒可處理的幀數

### 4. 工具比較指標
- **處理時間差異**：三種工具的平均處理時間
- **記憶體使用**：（可選）各工具的記憶體佔用
- **準確度一致性**：不同工具的偵測結果是否一致

---

## 📊 預期成果

### 量化成果
1. **120 組結構化實驗數據**（CSV 格式）
2. **統計分析報告**（包含平均值、標準差、信賴區間）
3. **效能基準數據**（各工具的處理時間分佈）

### 質性成果
1. **Claude 分析能力評估**：不同任務類型下的表現差異
2. **MCP 架構優勢分析**：相較於傳統方法的附加價值
3. **系統改進建議**：基於實驗結果提出優化方向

### 視覺化輸出
1. **場景複雜度 vs 準確率**：柱狀圖
2. **工具效能比較**：箱型圖
3. **時間分解圖**：堆疊圖（YOLO 推論 / MCP 傳輸 / Claude 分析）
4. **信心分數分佈**：直方圖

---

## 🔄 實驗執行檢查清單

### 實驗前
- [ ] ESP32-CAM 已固定並測試連線
- [ ] MCP 服務器運行正常
- [ ] Claude Desktop 已連接
- [ ] 實驗記錄系統已初始化
- [ ] 測試動物（貓/狗）已準備

### 實驗中
- [ ] 每次實驗前確認偵測畫面清晰
- [ ] 實時記錄異常狀況（網路中斷、偵測失敗等）
- [ ] 定期儲存數據（每 10 次）

### 實驗後
- [ ] 數據已完整儲存
- [ ] 進行人工驗證（標註 Ground Truth）
- [ ] 計算評估指標
- [ ] 生成視覺化圖表
- [ ] 撰寫實驗報告

---

## 📝 注意事項

1. **網路穩定性**：確保 ESP32-CAM 與電腦在同一網段，延遲 < 50ms
2. **動物福祉**：實驗過程中避免驚嚇或過度干擾動物
3. **數據備份**：每階段結束後立即備份 CSV 檔案
4. **異常處理**：若連續 3 次偵測失敗，暫停實驗並排查問題
5. **時間控制**：單次實驗不超過 3 小時，避免設備過熱或動物疲勞

---

## 🔗 相關文件

- [MCP 實作技術報告](mcp_implementation_technical_report.md)
- [MCP 工具使用指南](mcp_tools_usage_guide.md)
- [串流同步優化](stream_sync_optimization.md)
- [工作流程 v1.1](v1.1_workflow.md)

---

**文件版本控制**
- v1.0 (2025-11-06)：初版，定義實驗方法與流程
